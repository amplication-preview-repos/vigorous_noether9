/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { IncomeService } from "../income.service";
import { IncomeCreateInput } from "./IncomeCreateInput";
import { Income } from "./Income";
import { IncomeFindManyArgs } from "./IncomeFindManyArgs";
import { IncomeWhereUniqueInput } from "./IncomeWhereUniqueInput";
import { IncomeUpdateInput } from "./IncomeUpdateInput";

export class IncomeControllerBase {
  constructor(protected readonly service: IncomeService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Income })
  async createIncome(@common.Body() data: IncomeCreateInput): Promise<Income> {
    return await this.service.createIncome({
      data: data,
      select: {
        amount: true,
        createdAt: true,
        date: true,
        description: true,
        id: true,
        source: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Income] })
  @ApiNestedQuery(IncomeFindManyArgs)
  async incomes(@common.Req() request: Request): Promise<Income[]> {
    const args = plainToClass(IncomeFindManyArgs, request.query);
    return this.service.incomes({
      ...args,
      select: {
        amount: true,
        createdAt: true,
        date: true,
        description: true,
        id: true,
        source: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Income })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async income(
    @common.Param() params: IncomeWhereUniqueInput
  ): Promise<Income | null> {
    const result = await this.service.income({
      where: params,
      select: {
        amount: true,
        createdAt: true,
        date: true,
        description: true,
        id: true,
        source: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Income })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateIncome(
    @common.Param() params: IncomeWhereUniqueInput,
    @common.Body() data: IncomeUpdateInput
  ): Promise<Income | null> {
    try {
      return await this.service.updateIncome({
        where: params,
        data: data,
        select: {
          amount: true,
          createdAt: true,
          date: true,
          description: true,
          id: true,
          source: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Income })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteIncome(
    @common.Param() params: IncomeWhereUniqueInput
  ): Promise<Income | null> {
    try {
      return await this.service.deleteIncome({
        where: params,
        select: {
          amount: true,
          createdAt: true,
          date: true,
          description: true,
          id: true,
          source: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
